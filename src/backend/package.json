{
  "name": "tenantos-api-render",
  "private": true,
  "description": "Thin entry for Render when Root Directory is src/backend; API source is ../../backend",
  "scripts": {
    "build": "npm install --prefix ../../backend && npm run build --prefix ../../backend",
    "start": "node ../../backend/scripts/start.cjs"
  }
}
