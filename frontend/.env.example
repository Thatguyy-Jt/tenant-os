# Backend API (must include /api/v1). Optional if omitted: dev → localhost:4000, production build → tenant-os.onrender.com
# Local:
# VITE_API_BASE_URL=http://localhost:4000/api/v1
# Production (also set in Vercel → Environment Variables):
# VITE_API_BASE_URL=https://tenant-os.onrender.com/api/v1
